name: My First Example

on: Push

jobs:
    first_job:
        runs-on:  ubuntu-latest
        steps:
            - name: First Step
              run: echo "This is my first step"  #execute 

            - name: Second Step
              run: ls
  